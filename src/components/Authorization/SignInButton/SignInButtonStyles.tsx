import { makeStyles } from "@material-ui/core/styles";

const signInButtonStyles = makeStyles(
  {
    white: {
      color: "white",
      fontWeight: "normal",
    },
  },
);

export default signInButtonStyles;
